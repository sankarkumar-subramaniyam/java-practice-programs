/**
 * 
 */
/**
 * @author Sankar
 *
 */
package math;


// MEAN = AVG (Add all numbers and divide it by total no.of elements [= sum/ count] )
// MEDIAN  = MIDDLE (sort the numbers and find the value of middle index ( = total no.of elements / 2)
// MODE = maximum occurred number ( count occurrence and take max occurred value)
// Factorial(n) =  n * (n-1)!
// Fibonacci series till n = 1 + ((n-1) + n)